// Compatibility shim: legacy import path (tweets/dto/tweet.entity) used by
// some spec files; the real entity lives at tweets/entities/tweet.entity.
export { Tweet } from '../entities/tweet.entity';
